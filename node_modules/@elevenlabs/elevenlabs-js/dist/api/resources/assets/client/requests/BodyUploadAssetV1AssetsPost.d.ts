import type * as core from "../../../../../core";
/**
 * @example
 *     {
 *         asset: fs.createReadStream("/path/to/your/file"),
 *         name: "name"
 *     }
 */
export interface BodyUploadAssetV1AssetsPost {
    /** The file to upload. */
    asset: core.file.Uploadable;
    /** Display name for the asset. */
    name: string;
}
