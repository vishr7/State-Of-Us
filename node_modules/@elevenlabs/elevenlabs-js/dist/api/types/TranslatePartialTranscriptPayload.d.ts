/**
 * Interim transcription of the source audio.
 */
export interface TranslatePartialTranscriptPayload {
    /** The message type identifier. */
    messageType: "partial_transcript";
    /** Partial transcription text. */
    text: string;
}
