/**
 * @example
 *     {
 *         agentVersionId: "agent_version_id"
 *     }
 */
export interface ProceduresListRequest {
    /** The agent version ID to retrieve the procedure for. */
    agentVersionId?: string;
}
