/**
 * Translated text in the target language.
 */
export interface TranslateTranslationPayload {
    /** The message type identifier. */
    messageType: "translation";
    /** Translated text. */
    text: string;
}
