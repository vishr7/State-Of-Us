/**
 * The new revision after a source edit that returns no segment, such as a delete.
 */
export interface DubbingTranscriptRevisionResponse {
    /** The project's source-transcript revision after this edit. */
    revision: number;
}
