/**
 * Filter preset, distance (proximity EQ), and environment (convolution reverb).
 */
export interface EffectsSpecOutput {
    filterPresetId?: string;
    distance: number;
    environmentId?: string;
    backgroundNoiseId?: string;
    sendLevel: number;
    seed?: number;
}
